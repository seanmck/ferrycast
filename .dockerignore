.git
.github
.venv
venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache
*.egg-info

# Never bake collected data into the image — it belongs on the volume.
data/
*.db
*.db-wal
*.db-shm
.env

tests/
deploy/

# The logo master, the share-card source, and the scripts that cut them up. Everything they
# produce is committed under src/ and ships with the package; the 1 MB source render, and
# the Chrome that draws the card, have no business in the image.
brand/
