.git
.github
.venv
venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache
*.egg-info

# Never bake collected data into the image — it belongs on the volume.
data/
*.db
*.db-wal
*.db-shm
.env

tests/
deploy/
# Source for the share card. The rendered PNG lives under src/ and ships; the HTML it is
# drawn from, and the Chrome that draws it, are not needed in the image.
assets/
