.git
.github
.venv
venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache
*.egg-info

# Never bake collected data into the image — it belongs on the volume.
data/
*.db
*.db-wal
*.db-shm
.env

tests/
deploy/

# The logo master and the script that cuts it up. The assets it produces are committed
# under src/ and ship with the package; the 1 MB source render has no business in the image.
brand/
